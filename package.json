{
  "name": "office-to-pdf",
  "version": "0.1.0",
  "private": true,
  "description": "Монорепозиторий: конвертация DOCX/XLSX в PDF в браузере через LibreOffice WASM",
  "packageManager": "pnpm@11.21.0",
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "preview": "pnpm --filter web preview"
  }
}
